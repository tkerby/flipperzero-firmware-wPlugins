#include "src/cuberzero.h"

void SceneTimerOptionsEnter(const PCUBERZERO instance) {
	UNUSED(instance);
	/*variable_item_list_reset(instance->view.variableList);
	variable_item_list_set_enter_callback(instance->view.variableList, (VariableItemListEnterCallback) callbackEnter, instance);
	VariableItem* const item = variable_item_list_add(instance->view.variableList, "Cube", COUNT_CUBERZEROCUBE, callbackChange, instance);

	if(!item) {
		goto error;
	}

	variable_item_set_current_value_index(item, instance->settings.cube);
	variable_item_set_current_value_text(item, OptionCubes[instance->settings.cube]);
	view_dispatcher_switch_to_view(instance->dispatcher, CUBERZERO_VIEW_VARIABLE_ITEM_LIST);
	return;
error:
	scene_manager_handle_back_event(instance->manager);*/
}
