name: FAP Build & Auto Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

concurrency:
  group: auto-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          path: app
          fetch-depth: 0

      - uses: actions/checkout@v4
        with:
          repository: flipperdevices/flipperzero-firmware
          ref: dev
          path: firmware
          submodules: recursive

      - id: manifest
        shell: bash
        run: |
          set -euo pipefail
          APP_DIR_NAME="${GITHUB_REPOSITORY##*/}"
          DEST_DIR="firmware/applications_user/${APP_DIR_NAME}"

          mkdir -p "$DEST_DIR"
          rsync -a --delete --exclude '.git' app/ "$DEST_DIR/"

          mapfile -t APP_IDS < <(sed -n 's/^[[:space:]]*appid="\([^"]\+\)".*/\1/p' app/application.fam)
          if [ "${#APP_IDS[@]}" -eq 0 ]; then
            echo "No appid entries found in application.fam" >&2
            exit 1
          fi

          BUILD_TARGETS=()
          for APP_ID in "${APP_IDS[@]}"; do
            BUILD_TARGETS+=("fap_${APP_ID}")
          done

          {
            printf 'app_dir_name=%s\n' "$APP_DIR_NAME"
            printf 'build_targets=%s\n' "${BUILD_TARGETS[*]}"
            echo 'app_ids<<EOF'
            printf '%s\n' "${APP_IDS[@]}"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - shell: bash
        working-directory: firmware
        run: |
          set -euo pipefail
          ./fbt ${{ steps.manifest.outputs.build_targets }}

      - shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          while IFS= read -r APP_ID; do
            [ -n "$APP_ID" ] || continue
            find firmware/build -type f -name "${APP_ID}.fap" -exec cp {} artifacts/ \;
          done <<'EOF'
          ${{ steps.manifest.outputs.app_ids }}
          EOF

          if ! find artifacts -maxdepth 1 -type f -name '*.fap' | grep -q .; then
            echo "No FAP artifacts were collected" >&2
            exit 1
          fi

          ls -l artifacts

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.repository.name }}-${{ github.ref_name }}
          path: artifacts/*.fap

      - env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.ref_name }}"

          if gh release view "$TAG" --repo "$GH_REPO" >/dev/null 2>&1; then
            echo "Release already exists"
            exit 0
          fi

          gh release create "$TAG" \
            --repo "$GH_REPO" \
            --title "$TAG" \
            --notes "Automated build from ${{ github.sha }}" \
            artifacts/*.fap
