# Screenshots

`scanning.png` and `result.png` are placeholder images generated from pixel data.
Replace them with real device captures using `ufbt launch` and the Flipper's
screenshot function (hold **Up** on the main menu screen, or use qFlipper).

Expected dimensions: **128 × 64 pixels**, PNG.
