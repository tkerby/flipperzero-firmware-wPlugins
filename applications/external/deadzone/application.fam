App(
    appid="deadzone",
    name="DeadZone",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="game_app",
    stack_size=4 * 1024,
    fap_icon="icon.png",
    # fap_icon_assets="assets",
    fap_file_assets="assets",
    fap_extbuild=(
        ExtFile(
            path="${FAP_SRC_DIR}/assets",
            command="${PYTHON3} ${FAP_SRC_DIR}/engine/scripts/sprite_builder.py ${FAP_SRC_DIR.abspath}/sprites ${TARGET.abspath}/sprites",
        ),
    ),
    fap_category="Games",
    fap_author="retrooper",
    fap_weburl="https://github.com/retrooper/deadzone",
    fap_version="0.2",
    fap_description="Fight through tough challenges, dodge enemy fire, and avoid the falling obstacles to survive!",
)
