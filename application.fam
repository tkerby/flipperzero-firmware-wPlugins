App(
    appid="enc_reader",
    name="Encoder reader",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="enc_reader_app",
    cdefines=["ENC_READER"],
	requires=[
        "gui",
        "notifications",
    ],
    stack_size=1 * 1024,
    order=90,
    fap_icon="enc_reader_logo_10px.png",
    fap_category="GPIO",
)