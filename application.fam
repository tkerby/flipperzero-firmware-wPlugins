# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="stroboscope",  # Must be unique
    name="Stroboscope",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="stroboscope_app",
    stack_size=2 * 1024,
    fap_category="GPIO",
    # Optional values
    # fap_version="0.1",
    fap_icon="stroboscope.png",  # 10x10 1-bit PNG
    # fap_description="A simple app",
    # fap_author="J. Doe",
    # fap_weburl="https://github.com/user/stroboscope",
    fap_icon_assets="images",  # Image assets to compile for this application
)
