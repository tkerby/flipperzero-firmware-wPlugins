# Quick Build & Install Guide

## Quick Build with ufbt (Recommended)

```bash
# Install ufbt (one-time setup)
pip install ufbt

# Navigate to app directory
cd "d:\Scripts\Github Repos\flipperzero-lidar_emulator"

# Build and install to connected Flipper
ufbt launch
```
